var numerator = 10;
var denominator = 2;
var answer = (numerator / denominator);
console.log(answer);

var incrementer = 1;
var start = incrementer;

var end;
var difference;

incrementer += 1;
incrementer += 1;
incrementer += 1;

incrementer++;
incrementer++;
